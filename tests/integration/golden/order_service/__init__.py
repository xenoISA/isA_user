"""
Order Service Integration Tests (Golden)

Tests the OrderService layer with mocked dependencies (repository, event_bus).
These are NOT HTTP tests - they test the service business logic layer directly.
"""
