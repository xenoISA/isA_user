# Auto-generated from config/ports.yaml
# Service: calendar_service
# Regenerate with: python -m agents.tools.generate_config --deployment calendar_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar
  namespace: isa-cloud-staging
  labels:
    app: calendar
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calendar
  template:
    metadata:
      labels:
        app: calendar
    spec:
      containers:
      - name: calendar
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8217
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "calendar_service"
        - name: SERVICE_PORT
          value: "8217"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.calendar_service.main:app", "--host", "0.0.0.0", "--port", "8217"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8217
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8217
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: calendar
  namespace: isa-cloud-staging
  labels:
    app: calendar
spec:
  type: ClusterIP
  ports:
  - port: 8217
    targetPort: 8217
    protocol: TCP
    name: http
  selector:
    app: calendar
