# Auto-generated from config/ports.yaml
# Service: event_service
# Regenerate with: python -m agents.tools.generate_config --deployment event_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event
  namespace: isa-cloud-staging
  labels:
    app: event
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event
  template:
    metadata:
      labels:
        app: event
    spec:
      containers:
      - name: event
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8230
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "event_service"
        - name: SERVICE_PORT
          value: "8230"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.event_service.main:app", "--host", "0.0.0.0", "--port", "8230"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8230
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8230
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: event
  namespace: isa-cloud-staging
  labels:
    app: event
spec:
  type: ClusterIP
  ports:
  - port: 8230
    targetPort: 8230
    protocol: TCP
    name: http
  selector:
    app: event
