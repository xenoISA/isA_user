# Auto-generated from config/ports.yaml
# Service: project_service
# Regenerate with: python -m agents.tools.generate_config --deployment project_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: project
  namespace: isa-cloud-staging
  labels:
    app: project
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project
  template:
    metadata:
      labels:
        app: project
    spec:
      containers:
      - name: project
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8260
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "project_service"
        - name: SERVICE_PORT
          value: "8260"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.project_service.main:app", "--host", "0.0.0.0", "--port", "8260"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8260
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8260
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: project
  namespace: isa-cloud-staging
  labels:
    app: project
spec:
  type: ClusterIP
  ports:
  - port: 8260
    targetPort: 8260
    protocol: TCP
    name: http
  selector:
    app: project
