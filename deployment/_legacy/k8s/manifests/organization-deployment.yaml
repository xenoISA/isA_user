# Auto-generated from config/ports.yaml
# Service: organization_service
# Regenerate with: python -m agents.tools.generate_config --deployment organization_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: organization
  namespace: isa-cloud-staging
  labels:
    app: organization
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: organization
  template:
    metadata:
      labels:
        app: organization
    spec:
      containers:
      - name: organization
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8212
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "organization_service"
        - name: SERVICE_PORT
          value: "8212"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.organization_service.main:app", "--host", "0.0.0.0", "--port", "8212"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8212
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8212
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: organization
  namespace: isa-cloud-staging
  labels:
    app: organization
spec:
  type: ClusterIP
  ports:
  - port: 8212
    targetPort: 8212
    protocol: TCP
    name: http
  selector:
    app: organization
