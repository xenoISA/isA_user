# Auto-generated from config/ports.yaml
# Service: payment_service
# Regenerate with: python -m agents.tools.generate_config --deployment payment_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: isa-cloud-staging
  labels:
    app: payment
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
    spec:
      containers:
      - name: payment
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8207
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "payment_service"
        - name: SERVICE_PORT
          value: "8207"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.payment_service.main:app", "--host", "0.0.0.0", "--port", "8207"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8207
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8207
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: isa-cloud-staging
  labels:
    app: payment
spec:
  type: ClusterIP
  ports:
  - port: 8207
    targetPort: 8207
    protocol: TCP
    name: http
  selector:
    app: payment
