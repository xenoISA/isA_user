# Auto-generated from config/ports.yaml
# Service: document_service
# Regenerate with: python -m agents.tools.generate_config --deployment document_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: document
  namespace: isa-cloud-staging
  labels:
    app: document
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: document
  template:
    metadata:
      labels:
        app: document
    spec:
      containers:
      - name: document
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8227
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "document_service"
        - name: SERVICE_PORT
          value: "8227"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.document_service.main:app", "--host", "0.0.0.0", "--port", "8227"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8227
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8227
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: document
  namespace: isa-cloud-staging
  labels:
    app: document
spec:
  type: ClusterIP
  ports:
  - port: 8227
    targetPort: 8227
    protocol: TCP
    name: http
  selector:
    app: document
