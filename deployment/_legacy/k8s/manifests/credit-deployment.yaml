# Auto-generated from config/ports.yaml
# Service: credit_service
# Regenerate with: python -m agents.tools.generate_config --deployment credit_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: credit
  namespace: isa-cloud-staging
  labels:
    app: credit
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: credit
  template:
    metadata:
      labels:
        app: credit
    spec:
      containers:
      - name: credit
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8229
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "credit_service"
        - name: SERVICE_PORT
          value: "8229"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.credit_service.main:app", "--host", "0.0.0.0", "--port", "8229"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8229
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8229
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: credit
  namespace: isa-cloud-staging
  labels:
    app: credit
spec:
  type: ClusterIP
  ports:
  - port: 8229
    targetPort: 8229
    protocol: TCP
    name: http
  selector:
    app: credit
