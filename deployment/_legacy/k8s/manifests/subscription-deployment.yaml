# Auto-generated from config/ports.yaml
# Service: subscription_service
# Regenerate with: python -m agents.tools.generate_config --deployment subscription_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription
  namespace: isa-cloud-staging
  labels:
    app: subscription
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscription
  template:
    metadata:
      labels:
        app: subscription
    spec:
      containers:
      - name: subscription
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8228
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "subscription_service"
        - name: SERVICE_PORT
          value: "8228"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.subscription_service.main:app", "--host", "0.0.0.0", "--port", "8228"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8228
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8228
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: subscription
  namespace: isa-cloud-staging
  labels:
    app: subscription
spec:
  type: ClusterIP
  ports:
  - port: 8228
    targetPort: 8228
    protocol: TCP
    name: http
  selector:
    app: subscription
