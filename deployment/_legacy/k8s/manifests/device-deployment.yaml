# Auto-generated from config/ports.yaml
# Service: device_service
# Regenerate with: python -m agents.tools.generate_config --deployment device_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: device
  namespace: isa-cloud-staging
  labels:
    app: device
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: device
  template:
    metadata:
      labels:
        app: device
    spec:
      containers:
      - name: device
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8220
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "device_service"
        - name: SERVICE_PORT
          value: "8220"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.device_service.main:app", "--host", "0.0.0.0", "--port", "8220"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8220
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8220
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: device
  namespace: isa-cloud-staging
  labels:
    app: device
spec:
  type: ClusterIP
  ports:
  - port: 8220
    targetPort: 8220
    protocol: TCP
    name: http
  selector:
    app: device
