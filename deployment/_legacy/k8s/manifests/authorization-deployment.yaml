# Auto-generated from config/ports.yaml
# Service: authorization_service
# Regenerate with: python -m agents.tools.generate_config --deployment authorization_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authorization
  namespace: isa-cloud-staging
  labels:
    app: authorization
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authorization
  template:
    metadata:
      labels:
        app: authorization
    spec:
      containers:
      - name: authorization
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8204
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "authorization_service"
        - name: SERVICE_PORT
          value: "8204"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.authorization_service.main:app", "--host", "0.0.0.0", "--port", "8204"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8204
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8204
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: authorization
  namespace: isa-cloud-staging
  labels:
    app: authorization
spec:
  type: ClusterIP
  ports:
  - port: 8204
    targetPort: 8204
    protocol: TCP
    name: http
  selector:
    app: authorization
