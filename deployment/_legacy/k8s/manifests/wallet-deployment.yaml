# Auto-generated from config/ports.yaml
# Service: wallet_service
# Regenerate with: python -m agents.tools.generate_config --deployment wallet_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet
  namespace: isa-cloud-staging
  labels:
    app: wallet
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wallet
  template:
    metadata:
      labels:
        app: wallet
    spec:
      containers:
      - name: wallet
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8208
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "wallet_service"
        - name: SERVICE_PORT
          value: "8208"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.wallet_service.main:app", "--host", "0.0.0.0", "--port", "8208"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8208
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8208
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: wallet
  namespace: isa-cloud-staging
  labels:
    app: wallet
spec:
  type: ClusterIP
  ports:
  - port: 8208
    targetPort: 8208
    protocol: TCP
    name: http
  selector:
    app: wallet
