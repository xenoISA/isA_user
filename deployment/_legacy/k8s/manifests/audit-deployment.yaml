# Auto-generated from config/ports.yaml
# Service: audit_service
# Regenerate with: python -m agents.tools.generate_config --deployment audit_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit
  namespace: isa-cloud-staging
  labels:
    app: audit
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit
  template:
    metadata:
      labels:
        app: audit
    spec:
      containers:
      - name: audit
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8205
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "audit_service"
        - name: SERVICE_PORT
          value: "8205"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.audit_service.main:app", "--host", "0.0.0.0", "--port", "8205"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8205
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8205
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: audit
  namespace: isa-cloud-staging
  labels:
    app: audit
spec:
  type: ClusterIP
  ports:
  - port: 8205
    targetPort: 8205
    protocol: TCP
    name: http
  selector:
    app: audit
