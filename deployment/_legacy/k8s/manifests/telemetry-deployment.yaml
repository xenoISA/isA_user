# Auto-generated from config/ports.yaml
# Service: telemetry_service
# Regenerate with: python -m agents.tools.generate_config --deployment telemetry_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry
  namespace: isa-cloud-staging
  labels:
    app: telemetry
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telemetry
  template:
    metadata:
      labels:
        app: telemetry
    spec:
      containers:
      - name: telemetry
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8225
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "telemetry_service"
        - name: SERVICE_PORT
          value: "8225"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.telemetry_service.main:app", "--host", "0.0.0.0", "--port", "8225"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8225
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8225
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: telemetry
  namespace: isa-cloud-staging
  labels:
    app: telemetry
spec:
  type: ClusterIP
  ports:
  - port: 8225
    targetPort: 8225
    protocol: TCP
    name: http
  selector:
    app: telemetry
