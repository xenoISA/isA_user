# Auto-generated from config/ports.yaml
# Service: task_service
# Regenerate with: python -m agents.tools.generate_config --deployment task_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: task
  namespace: isa-cloud-staging
  labels:
    app: task
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task
  template:
    metadata:
      labels:
        app: task
    spec:
      containers:
      - name: task
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8211
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "task_service"
        - name: SERVICE_PORT
          value: "8211"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.task_service.main:app", "--host", "0.0.0.0", "--port", "8211"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8211
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8211
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: task
  namespace: isa-cloud-staging
  labels:
    app: task
spec:
  type: ClusterIP
  ports:
  - port: 8211
    targetPort: 8211
    protocol: TCP
    name: http
  selector:
    app: task
