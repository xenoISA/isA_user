# Auto-generated from config/ports.yaml
# Service: location_service
# Regenerate with: python -m agents.tools.generate_config --deployment location_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location
  namespace: isa-cloud-staging
  labels:
    app: location
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location
  template:
    metadata:
      labels:
        app: location
    spec:
      containers:
      - name: location
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8224
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "location_service"
        - name: SERVICE_PORT
          value: "8224"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.location_service.main:app", "--host", "0.0.0.0", "--port", "8224"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8224
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8224
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: location
  namespace: isa-cloud-staging
  labels:
    app: location
spec:
  type: ClusterIP
  ports:
  - port: 8224
    targetPort: 8224
    protocol: TCP
    name: http
  selector:
    app: location
