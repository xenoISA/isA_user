# Auto-generated from config/ports.yaml
# Service: storage_service
# Regenerate with: python -m agents.tools.generate_config --deployment storage_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage
  namespace: isa-cloud-staging
  labels:
    app: storage
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storage
  template:
    metadata:
      labels:
        app: storage
    spec:
      containers:
      - name: storage
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8209
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "storage_service"
        - name: SERVICE_PORT
          value: "8209"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.storage_service.main:app", "--host", "0.0.0.0", "--port", "8209"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8209
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8209
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: storage
  namespace: isa-cloud-staging
  labels:
    app: storage
spec:
  type: ClusterIP
  ports:
  - port: 8209
    targetPort: 8209
    protocol: TCP
    name: http
  selector:
    app: storage
