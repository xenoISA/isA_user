# Auto-generated from config/ports.yaml
# Service: memory_service
# Regenerate with: python -m agents.tools.generate_config --deployment memory_service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory
  namespace: isa-cloud-staging
  labels:
    app: memory
    tier: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memory
  template:
    metadata:
      labels:
        app: memory
    spec:
      containers:
      - name: memory
        image: isa-user-base:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8223
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: SERVICE_NAME
          value: "memory_service"
        - name: SERVICE_PORT
          value: "8223"
        command: ["python", "-m", "uvicorn"]
        args: ["microservices.memory_service.main:app", "--host", "0.0.0.0", "--port", "8223"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8223
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8223
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: memory
  namespace: isa-cloud-staging
  labels:
    app: memory
spec:
  type: ClusterIP
  ports:
  - port: 8223
    targetPort: 8223
    protocol: TCP
    name: http
  selector:
    app: memory
