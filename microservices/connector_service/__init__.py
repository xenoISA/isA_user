"""
Connector Service Microservice

Owns the connector marketplace: built-in connector catalog, per-user install
state, and custom remote MCP connectors (BYO MCP servers). Backend home for
the routes the ConnectorMarketplace UI in isA_ has been calling into the
void — see xenoISA/isA_#464.
"""

__version__ = "1.0.0"
